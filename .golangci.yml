linters:
  enable:
    - staticcheck
    - gosec
  disable:
    - deadcode
    - unused
    - gosimple
  exclude-use-default: false
linters-settings:
  staticcheck:
    go: "1.17"
    checks: ["all"]
